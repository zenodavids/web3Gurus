{
  "name": "molograph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ zenodavids/molograph",
    "create-local": "graph create --node http://localhost:8020/ zenodavids/molograph",
    "remove-local": "graph remove --node http://localhost:8020/ zenodavids/molograph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 zenodavids/molograph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.42.2",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
